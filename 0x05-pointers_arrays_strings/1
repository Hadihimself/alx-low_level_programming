#include "main.h"
#include "string.h"

/**
* _puts - this function prints s
* @s: String to count
* Return: String length
*/

void print_rev(char *s)
{
	int i;

	for (i = strlen(s); s[i] != '\0'; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
